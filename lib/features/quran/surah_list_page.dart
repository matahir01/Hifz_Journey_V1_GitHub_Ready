import 'package:flutter/material.dart';

import 'quran_hub_page.dart';

/// Backwards-compatible entry point retained for older routes.
class SurahListPage extends StatelessWidget {
  const SurahListPage({super.key});

  @override
  Widget build(BuildContext context) => const QuranHubPage();
}

import 'package:flutter/material.dart';import '../../data/models/surah.dart';import '../../data/models/ayah.dart';import '../../data/repositories/quran_repository.dart';import '../../core/database/app_database.dart';import 'package:provider/provider.dart';
class SurahPage extends StatelessWidget{final Surah surah;const SurahPage({super.key,required this.surah});@override Widget build(BuildContext c)=>Scaffold(appBar:AppBar(title:Text(surah.nameEn)),body:FutureBuilder<List<Ayah>>(future:QuranRepository(AppDatabase.instance).ayahsForSurah(surah.id),builder:(c,s){if(!s.hasData)return const Center(child:CircularProgressIndicator());return ListView.separated(padding:const EdgeInsets.all(16),itemCount:s.data!.length,itemBuilder:(c,i){final a=s.data![i];return Container(padding:const EdgeInsets.fromLTRB(16,18,16,18),decoration:BoxDecoration(borderRadius:BorderRadius.circular(18),color:Theme.of(c).colorScheme.surfaceContainerHighest.withOpacity(.35)),child:Column(crossAxisAlignment:CrossAxisAlignment.stretch,children:[Row(children:[CircleAvatar(radius:16,child:Text('${a.ayahNumber}',style:const TextStyle(fontSize:11))),const Spacer(),IconButton(onPressed:(){},icon:const Icon(Icons.bookmark_border))]),const SizedBox(height:10),Text(a.textUthmani,textAlign:TextAlign.right,textDirection:TextDirection.rtl,style:const TextStyle(fontSize:29,height:1.9)),const SizedBox(height:8),Text('Page ${a.page} • Juz ${a.juz}',textAlign:TextAlign.left,style:Theme.of(c).textTheme.bodySmall)]) ;},separatorBuilder:(_,_)=>const SizedBox(height:10));}));}
